# Open Workspace Settings (JSON)

This is a VS Code Extension for opening your workspace settings in JSON format.

## Usage

- Open Command Palette by pressing `Command + Shift + P`, then search for `Open Workspace Settings (JSON)`.

- You can also set this extension to your keyboard shortcuts (`Command + K + S`) to open settings with key bindings of your choise.
